import { cards, startingDeck } from './cards'
import monsters from './monsters'
import player from './player'

export default {
  startingHandCount: 5,
  startingDeck,
  cards,
  monsters,
  player,
}
